/*

-> when we made frontend and backend separately, we need to terminals to run those .
-> second , no shared types , like we need to define Todo type in both frontend and backend , 
   which is not ideal . we can use turborepo to share types between frontend and backend 
   and run both with one command .
-> In next chap or step we will see how to use turborepo to share types and run both frontend
   and backend with one command .


*/
